THE TEN 2026-09-23 IN-VPC RE-SMOKE TURNS, BANKED FOR tests/unit/test_chain_lints.py (lane A, 09-23 fix round).
Origin: s3 baseline_eval_queries_state_resmoke_v1_anthropic_20260923T15*.json per_answer rows (image = HEAD
ee06f19c), pulled read-only to the fix round's scratch and reduced to what answer._chain_lints and
answer._chain_backstop read: the writer's POST-VERIFY tldr/mechanism (raw_draft.postverify_*), the board's
anchors, every row's (contract, driver_id, series_key), the RENDERED chains of the state_board trace payload,
and EVERY served number call by its [N] index, each reduced to its headline row
(citations.headline_row -- the label head and headline value are the served call's own).
ONE TRANSFORMATION: non-ASCII prose characters folded to ASCII (em dash -> --, n-tilde -> n, ...), the 09-16
corpus rule. ONE DERIVED FIELD: each measured hop's level handle `n`, located as the SB-1 level call of the
same card, scope and value followed by its percentile call; VALIDATED -- HEAD's own pass on this
reconstruction reproduces the ten served pages' L1 appends character for character.
These are UNSEEN real-seat prose for the L1 rule (written before the rule), the 09-15 negative-corpus law.
