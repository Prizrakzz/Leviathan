# WASDE manifest — generated by:
#   python jobs/ingest/fetch_usda_wasde.py --discover
#
# DO NOT edit manually. Re-run --discover to refresh.
#
# Fields per entry:
#   release_date   YYYY-MM-DD — date the WASDE was published
#   calendar_month YYYY-MM    — deduplicated calendar month key
#   mmyy           MMYY       — two-digit month + two-digit year (S3 key component)
#   fmt            txt|pdf    — file format (txt=1995-1999; pdf=all other years)
#   url            https://   — direct download URL from esmis.nal.usda.gov
#   filename       wasde{mmyy}.{fmt} — normalized filename used in S3 key

reports:
